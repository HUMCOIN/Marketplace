var ***REMOVED***ten = require('./***REMOVED***ten'),
    overRest = require('./_overRest'),
    setToString = require('./_setToString');

/**
 * A specialized version of `baseRest` which ***REMOVED***tens the rest array.
 *
 * @private
 * @param {Function} func The function to apply a rest parameter to.
 * @returns {Function} Returns the new function.
 */
function ***REMOVED***Rest(func) {
  return setToString(overRest(func, undefined, ***REMOVED***ten), func + '');
}

module.exports = ***REMOVED***Rest;
