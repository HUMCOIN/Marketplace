var baseFlatten = require('./_baseFlatten'),
    toInteger = require('./toInteger');

/**
 * Recursively ***REMOVED***ten `array` up to `depth` times.
 *
 * @static
 * @memberOf _
 * @since 4.4.0
 * @category Array
 * @param {Array} array The array to ***REMOVED***ten.
 * @param {number} [depth=1] The maximum recursion depth.
 * @returns {Array} Returns the new ***REMOVED***tened array.
 * @example
 *
 * var array = [1, [2, [3, [4]], 5]];
 *
 * _.***REMOVED***tenDepth(array, 1);
 * // => [1, 2, [3, [4]], 5]
 *
 * _.***REMOVED***tenDepth(array, 2);
 * // => [1, 2, 3, [4], 5]
 */
function ***REMOVED***tenDepth(array, depth) {
  var length = array == null ? 0 : array.length;
  if (!length) {
    return [];
  }
  depth = depth === undefined ? 1 : toInteger(depth);
  return baseFlatten(array, depth);
}

module.exports = ***REMOVED***tenDepth;
